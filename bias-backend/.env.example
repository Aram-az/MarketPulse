# Comma-separated list. Use * during hackathon if you want.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# File upload limit (MB)
MAX_FILE_MB=50

# If true, enables extra debug fields in responses
DEBUG=false